// #include is a pre processor command that tells the compiler to include the contents of a file
// <stdio.h> is a file that contains some standard input/output functions that can now be used once included
#include <stdio.h>

// main function -
// where the execution of program begins
int main(){

    // prints welcome message with \n at the end that creates a new line
    printf("Hello World\n");
    printf("The quick brown fox jumps over the lazy dog.\n");

    //the below function will return 0 if no errors but if errors will return a 1
    return 0;
}